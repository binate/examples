10 REM INPUT several values of mixed type on ONE reply line: a count and a
20 REM name, comma-separated. The whole reply is read at once and the two
30 REM data are assigned in order.
40 PRINT "ENTER COUNT, NAME";
50 INPUT N, N$
60 PRINT N; "COPIES FOR "; N$
70 END
