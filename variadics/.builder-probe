// Builder-feature probe for scripts/builder-gate.sh. This is a minimal program
// using the ONE language feature this example requires — a variadic parameter
// (`...int`) — deliberately NOT named *.bn so hygiene does not scan it.
//
// The generic CI sweeps (build-all, test-all, lint) and this example's own
// tests/run.sh compile this probe with the resolved bnc. If it fails to compile,
// the resolved bnc predates variadics (they landed after bnc-0.0.10, the pinned
// BUILDER_VERSION) and the example is skipped. Once BUILDER_VERSION names a
// release with variadics — or when BINATE_BUNDLE points at a main build — the
// probe compiles and the example activates automatically, no edit needed.
package "main"

func probe(xs ...int) int {
	return len(xs)
}

func main() {
	println(probe(1, 2, 3))
}
