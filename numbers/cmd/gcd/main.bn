package "main"

// A second runnable in the same example, sharing the same pkg/seq.
import "pkg/seq"
import "pkg/stdx/fmt"

// main prints the GCD of two sample integer pairs, one per line.
func main() {
	fmt.Println(seq.Gcd(48, 36))
	fmt.Println(seq.Gcd(1071, 462))
}
